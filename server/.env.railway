# Server Configuration for Railway
PORT=5001
NODE_ENV=production

# MongoDB Connection (already set in Railway environment variables)
# MONGODB_URI is set in Railway environment variables

# JWT Configuration (already set in Railway environment variables)
# JWT_SECRET is set in Railway environment variables
JWT_EXPIRATION=7d

# Google OAuth (already set in Railway environment variables)
# GOOGLE_CLIENT_ID is set in Railway environment variables
# GOOGLE_CLIENT_SECRET is set in Railway environment variables

# Frontend URL (for CORS)
FRONTEND_URL=https://task-manager-frontend-app.netlify.app
